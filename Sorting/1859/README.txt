First programme will scramble the words in the array to randomised order using Collections.shuffle();
Then it will count the number of words and append it at the end of each word
The second programme will take a shuffled randomised String with a integer appended at the each word. 
It will sort the arrays in order before removing the number that is appended in each word. 


